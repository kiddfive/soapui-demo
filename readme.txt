Run SOAPUI test case at local with below command:

mvn com.smartbear.soapui:soapui-maven-plugin:5.1.2:test

Or you can run it from Jenkins as a Maven project, and add above command in the script option.